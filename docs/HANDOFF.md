# Handoff

Full handoff is ticket **gd-lee5**: `.tickets/gd-lee5.md` (or `tk show gd-lee5` from the repo root).

Written 2026-09-18 04:37, updated in the ticket's notes through 08:00 the same day: pro-zekx closed
(default head stays `heads/v3.safetensors`), gd-ngu7 closed (Metal is the default encoder and device),
gd-eok7 open (Metal encoder overhead; the four planned items are done, the batched-throughput target is not).
